# Research Monitor Environment Configuration
# Copy this file to .env and customize for your environment

# ==================== SERVER CONFIGURATION ====================

# Server port (default: 5558)
RESEARCH_MONITOR_PORT=5558

# API authentication key (recommended for production)
# Leave empty for development (no authentication required)
RESEARCH_MONITOR_API_KEY=

# Flask secret key (auto-generated if not set)
# Set this in production for session security
RESEARCH_MONITOR_SECRET=

# ==================== DATABASE CONFIGURATION ====================

# SQLite database path (relative to server directory)
RESEARCH_DB_PATH=../unified_research.db

# ==================== FILESYSTEM PATHS ====================

# Timeline events directory (relative to server directory)
# Should point to timeline/data/events/
TIMELINE_EVENTS_PATH=../../timeline/data/events

# Research priorities directory (relative to server directory)
RESEARCH_PRIORITIES_PATH=../data/research_priorities

# Validation logs directory (relative to server directory)
VALIDATION_LOGS_PATH=../../timeline/data/validation_logs

# ==================== RESEARCH ORCHESTRATION ====================

# Number of events to stage before signaling commit (default: 10)
COMMIT_THRESHOLD=10

# ==================== CACHING CONFIGURATION ====================

# Cache type: simple, filesystem, redis, memcached (default: simple)
CACHE_TYPE=simple

# Default cache timeout in seconds (default: 300 = 5 minutes)
CACHE_DEFAULT_TIMEOUT=300

# Maximum number of cached items (default: 1000)
CACHE_THRESHOLD=1000

# ==================== NOTES ====================

# For development:
# - Use default values (no .env file needed)
# - API key can be empty (no authentication)
# - Paths are pre-configured for repository structure

# For production:
# - Set RESEARCH_MONITOR_API_KEY to a strong random value
# - Set RESEARCH_MONITOR_SECRET to a strong random value
# - Consider using absolute paths for stability
# - Enable Redis or Memcached for better caching
# - Set up proper log rotation
# - Use a process manager (systemd, supervisord)
